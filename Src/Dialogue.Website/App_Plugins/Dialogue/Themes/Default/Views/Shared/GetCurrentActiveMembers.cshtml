
@model Dialogue.Logic.Models.ViewModels.ActiveMembersViewModel

@if (Model.ActiveMembers.Any())
{
    <ul class="activememberslist">
        <li class="activememberslistheading">@Html.Lang("Stats.FooterActiveUsersText")</li>
        @foreach (var member in Model.ActiveMembers)
        {
            <li>
                <a title="@Html.Raw(member.UserName)" href="@member.NiceUrl">
                    <img src="@member.MemberImage(AppConstants.GravatarLeaderboardSize)" alt="@Html.Raw(member.UserName)" />
                </a>
            </li>
        }
    </ul>

}